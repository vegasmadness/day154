# Supabase Configuration
# Get these values from your Supabase project dashboard

# Public Supabase URL (safe to expose to client)
PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public Supabase Anonymous Key (safe to expose to client)
# This key has limited permissions based on your RLS policies
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose to client!)
# Only use this for server-side operations and migrations
# Keep this secret and never commit to version control
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Development Configuration
# Set to 'development' for local development, 'production' for live site
NODE_ENV=development

# Security Configuration
# Generate a strong random string for session encryption
# You can use: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here

# CORS Configuration (optional)
# Comma-separated list of allowed origins for CORS
# ALLOWED_ORIGINS=http://localhost:4321,https://yourdomain.com

# Rate Limiting Configuration (optional)
# Maximum requests per minute per IP
# RATE_LIMIT_MAX=100

# File Upload Configuration (optional)
# Maximum file size in bytes (default: 5MB)
# MAX_FILE_SIZE=5242880

# Logging Configuration (optional)
# Set to 'debug', 'info', 'warn', or 'error'
# LOG_LEVEL=info

# ============================================
# SECURITY NOTES:
# ============================================
# 1. NEVER commit .env files to version control
# 2. Use different keys for development and production
# 3. Rotate keys regularly
# 4. Keep SERVICE_ROLE_KEY absolutely secret
# 5. Use environment variables in production (Netlify, Vercel, etc.)
# 6. Validate all environment variables on startup
# ============================================